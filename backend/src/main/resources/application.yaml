spring:
  application:
    name: quran-memorization-map

  datasource:
    url: jdbc:postgresql://localhost:5432/quran_map
    username: quran
    password: quran

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true